import React from 'react';
import { useState } from 'react';
import { useEffect } from 'react';
import CoursesSummaryCard from '../../Shared/CoursesSummaryCard/CoursesSummaryCard';

const RecentCourses3 = () => {
    
    return (
        <div>
            {/* <h2 className='text-center text-primary'>Recently Added Courses</h2> */}
            {/* <div className='course_alignment'>
                {
                    courses.map(course => <CoursesSummaryCard key={course.id} courses={course}></CoursesSummaryCard>)
                }
            </div> */}
        </div>
    );
};

export default RecentCourses3;